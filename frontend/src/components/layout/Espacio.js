import React from 'react';

const Espacio = (props) => {
    const divStyle = {
        height: props.height,
    };
    return (
            <div style={divStyle}></div>
            );
}

export default Espacio;